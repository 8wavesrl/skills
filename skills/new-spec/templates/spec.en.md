# Spec title

> **Status**: Draft (not implemented)
> **Date**: 2026-01-01
> **Scope**: One or two lines summarizing what this spec introduces and what it touches.
> **Related specs**: [`other-spec.md`](./other-spec.md) (relationship), or "None"

<!--
Conventions:
- Language: English (prose, headings, comments). Identifiers, file/API names and product
  terms stay as they are.
- No em or en dashes (—/–): use a colon, parentheses, a comma or a new sentence.
- Status: "Draft (not implemented)" | "Partial (<what is done>)" | "Released".
  When a spec is 100% complete move it to `released/`.
  When it is obsolete or superseded move it to `archived/`.
- Date: creation date of the document (YYYY-MM-DD).
- Optional extra lines in the header block, after the four standard ones, for
  spec-specific metadata (e.g. `> **Version**: 0.1`, `> **Wave**: ...`).
-->

## Overview

Context, the problem to solve and a summary of the proposed solution.

## Goals and non-goals

- **Goals**: ...
- **Non-goals**: ...

## Design

Data model, services/tools involved, execution flow. List the **exact paths** of the files
to create or modify.

## Rollout phases

Incremental breakdown with verification criteria for each phase.
